# API-Only Node Profile - Only produces jobs, does not process them
# Use this profile when you want a dedicated API node without worker capabilities
platform.jobs.worker.enabled=false

# Database (override in production)
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=jobs;encrypt=false;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=YourStrong!Passw0rd
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
