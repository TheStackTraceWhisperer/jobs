# Spring Cloud Bootstrap Configuration
# This file is loaded before application.properties when spring-cloud-starter-bootstrap is on the classpath

# Application name (used by Spring Cloud Config)
spring.application.name=job-platform-reference-app

# Spring Cloud Config (Optional - uncomment to enable)
# spring.cloud.config.uri=http://localhost:8888
# spring.cloud.config.fail-fast=false
# spring.cloud.config.enabled=false

# Note: When using Spring Cloud Config Server, the job worker properties
# can be refreshed dynamically using the /actuator/refresh endpoint
# Example: POST http://localhost:8080/actuator/refresh
