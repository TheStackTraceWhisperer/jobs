# API Node Profile - For nodes that expose REST APIs
# Can be combined with worker profile for nodes that do both

# Database (override in production)
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=jobs;encrypt=false;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=YourStrong!Passw0rd
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
