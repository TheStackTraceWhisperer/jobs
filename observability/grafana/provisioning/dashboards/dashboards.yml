apiVersion: 1

providers:
  - name: 'Job Platform Dashboards'
    orgId: 1
    folder: 'Job Platform'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
